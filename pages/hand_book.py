import streamlit as st
with st.expander("Class 1 課堂筆記"):
    st.write(
        """
# 🐍 Python 筆記

## 1. `print()`

### 🌟 它是做什麼的？

`print()` 就像是在跟電腦說：

> 「請把這句話顯示在螢幕上！」

### 💻 範例

```python
print("Hello")
```

### 🖥️ 執行結果

```
Hello
```

### 📝 小提醒

- 文字要放在雙引號 `"` 或單引號 `'` 裡面。
- 少了括號或引號，程式就會出錯。

---

## 2. `input()`

### 🌟 它是做什麼的？

讓電腦問使用者問題，等使用者輸入答案。

```python
name = input("請輸入你的名字：")
print("你好", name)
```

---

## 🎯 今天重點

✅ `print()`：把東西顯示出來。
✅ `input()`：讓使用者輸入資料。
✅ `=`：把資料放進變數裡保存。

---

"""
    )

with st.expander("Class 2 課堂筆記"):
    st.write(
        """好的！以下是將 **2.1（for 迴圈）**、**2.2（range()）**、**2.3（迴圈變數 i）** 整合成一個完整章節，內容適合國小學生閱讀，避免重複介紹，閱讀起來也比較順暢。

# 2. Python 的 for 迴圈

## 🎯 什麼是 for 迴圈？

有時候，我們希望電腦一直重複做同一件事情，例如：

* 印出 5 次「Hello」
* 數數 0 到 4
* 重複畫出相同的圖形

如果每次都自己寫一次程式，會很麻煩，所以我們可以使用 **`for` 迴圈**。

`for` 的功能就是：

> **讓電腦按照順序，把同一件事情重複做好幾次。**

---

## 🎯 for、in、range() 一起使用

`for` 通常會搭配 **`in`** 和 **`range()`** 一起使用。

基本寫法：

```python
for i in range(5):
    print(i)
```

這一行程式可以分成三個部分：

### ① `for`

表示開始進行重複工作。

### ② `in`

表示從一串資料裡，一個一個拿出資料。

### ③ `range(5)`

`range()` 會產生一串連續的數字。

```python
range(5)
```

會得到：

```text
0、1、2、3、4
```

⚠️ 注意：

`range(5)` **不會包含 5**。

也就是說：

| 程式          | 產生的數字     |
| ----------- | --------- |
| `range(3)`  | 0、1、2     |
| `range(5)`  | 0、1、2、3、4 |
| `range(10)` | 0～9       |

可以記住一句話：

> **`range(數字)` 會從 0 開始，到「數字前一個」結束。**

---

## 🎯 什麼是迴圈變數？

程式中的：

```python
for i in range(5):
```

`i` 稱為**迴圈變數**。

它就像一個小盒子，每一次都會放進不同的數字。

| 第幾次 | i 的值 |
| --- | ---- |
| 第一次 | 0    |
| 第二次 | 1    |
| 第三次 | 2    |
| 第四次 | 3    |
| 第五次 | 4    |

每跑一次迴圈，`i` 都會自動換成下一個數字。

其實它不一定要叫 **`i`**，也可以自己取名字，例如：

```python
for number in range(5):
    print(number)
```

或

```python
for student in range(5):
    print(student)
```

只要名稱有意義、自己看得懂即可。

---

## 🎯 程式執行過程

程式：

```python
for i in range(5):
    print(i)
```

電腦會依照下面的順序執行：

第一次：

```text
i = 0
印出 0
```

第二次：

```text
i = 1
印出 1
```

第三次：

```text
i = 2
印出 2
```

第四次：

```text
i = 3
印出 3
```

第五次：

```text
i = 4
印出 4
```

最後畫面會看到：

```text
0
1
2
3
4
```

---

## 💡 生活中的例子

想像老師說：

> 「請第一位到第五位同學依序站起來。」

老師會按照順序叫：

1️⃣ 第一位同學
2️⃣ 第二位同學
3️⃣ 第三位同學
4️⃣ 第四位同學
5️⃣ 第五位同學

`for` 迴圈也是一樣，它會按照順序，一次拿出一個數字來使用。

---

## 📝 本章重點

✅ `for`：讓程式重複執行。

✅ `in`：從一串資料中，一個一個取出資料。

✅ `range(5)`：產生 **0、1、2、3、4**，不包含 5。

✅ `i`：迴圈變數，每次都會拿到一個新的數字。

✅ `print(i)`：把目前拿到的數字顯示在畫面上。

---

## 🎯 一句話記住

> **`for` 迴圈就像老師依照座號點名，電腦會依序拿出每個數字，並重複完成同一件事情。**

這樣整理後，**2.1、2.2、2.3 已經合併成一個完整章節**，閱讀流程會是：
**先認識 for → 再了解 in 和 range() → 再學迴圈變數 i → 最後看執行過程與生活例子**，比拆成三小節更適合國小學生學習。
""")
with st.expander("Class 3 課堂筆記"):
    st.write("""
這是一份專門為你設計的 **Python 魔法筆記**！我們用最簡單、好懂的方式，把今天學到的厲害指令通通記下來。準備好我們就開始囉！✨

---

## 🚂 第一站：自動幫你重複做事的「迴圈小火車」

當你想要電腦幫你重複畫畫、數數，寫很多次同樣程式碼太累了！這時候就要派出 **`for` 迴圈**。

### 1. `range()`：小火車的神秘軌道

`for i in range(...)` 就像一列小火車，`i` 是火車駕駛員（也可以叫別的名字），它會沿著 `range()` 鋪好的軌道一站一站載數字。

* **單向軌道 `range(5)**`：
火車會從 `0` 開始出發，**但是最後一站不載人**（不包含 5）！
所以載到的數字是：`0, 1, 2, 3, 4`。
* **指定起點與終點 `range(1, 5)**`：
火車從 `1` 出發，一樣不包含終點 `5`！
所以載到：`1, 2, 3, 4`。
* **跳格軌道 `range(1, 10, 2)**`：
從 `1` 出發，每次「走 2 步」（步進值），一樣不包含 `10`。
所以載到：`1, 3, 5, 7, 9`（像在跳格子一樣）。

```python
# 這是你學到的範例：把火車載到的數字通通乘兩倍！
for i in range(5):
    a = i * 2  # 把載到的數字乘以 2 存進 a
    print(a)   # 畫面上會出現：0, 2, 4, 6, 8

```

---

## 🎈 第二站：網頁神奇魔法與「猜數字遊戲」

我們可以用 `Streamlit` 這個神奇黏土，在網頁上做出可以按的按鈕、輸入框，甚至是好玩的遊戲！

### 1. 網頁的記憶吐司 `st.session_state`

一般的網頁只要一按按鈕就會「失憶」（重新整理），但如果我們用 `st.session_state`，網頁就會像吃了記憶吐司一樣，把重要的事情（比如：秘密數字、猜了幾次）牢牢記住！

```python
# 檢查如果「記憶」裡還沒有秘密數字，就隨機選一個 1~100 的數字記起來
if "secret_number" not in st.session_state:
    st.session_state.secret_number = random.randint(1, 100)

```

當玩家猜對時，我們還可以用 `st.balloons()` 魔法，在網頁上**噴出繽紛的慶祝氣球**喔！🎈

---

## 🔑 第三站：貼了標籤的百寶箱「字典 dict」

平常我們用列表（List）裝玩具，但當東西太多時，找起來很麻煩。**字典（dict）**就像一個百寶箱，裡面有很多抽屜，每個抽屜都有**專屬的鑰匙（Key）**，鎖著**珍貴的寶物（Value）**。

> **黃金規則：**
> * 鑰匙（Key）不能重複，必須是獨一無二的！
> * 寶物（Value）可以是任何東西，重複也沒關係。
> 
> 

### 1. 字典基本功法

我們用一個簡單的百寶箱 `d = {"a": 1, "b": 2, "c": 3}` 來練習：

* **看有哪些鑰匙？** `d.keys()`：拿出一整串鑰匙（會拿到 `"a"`, `"b"`, `"c"`）。
* **看有哪些寶物？** `d.values()`：打開所有抽屜看裡面裝什麼（會拿到 `1`, `2`, `3`）。
* **塞新寶物進去**：
```python
d["d"] = 4  # 新增一個叫 "d" 的抽屜，裝進寶物 4
d["a"] = 5  # 把舊抽屜 "a" 裡面的寶物換成全新的 5

```


* **丟掉抽屜** `d.pop("a")`：把 `"a"` 抽屜連同裡面的寶物一起丟掉！
* **檢查鑰匙在不在？** `in` 偵探：
```python
print("a" in d)  # 檢查百寶箱裡有沒有 "a" 的鑰匙？有就回答 True（對），沒有就回答 False（錯）

```



---

## 🏫 第四站：大挑戰！學校成績超級搜尋大師

現在，我們要用百寶箱字典來裝「好朋友們的成績」。這是一個「百寶箱裡還有小百寶箱」的超級字典（巢狀字典）：

```python
# 每個同學的抽屜裡，又裝了國文、數學、英文三個學科的成績抽屜！
grade = {
    "小明": {"國文": [90, 80, 70], "數學": [85, 75, 65], "英文": [95, 85, 75]},
    "小美": {"國文": [88, 78, 68], "數學": [83, 73, 63], "英文": [93, 83, 73]},
}

```

### 🎯 尋寶特攻隊：怎麼拿到特定成績？

1. **找出小明的數學成績**：
先開「小明」的抽屜，再開裡面的「數學」抽屜。
`grade["小明"]["數學"]` ➡️ 得到 `[85, 75, 65]`
2. **找出小美第一次（第 0 個）英文成績**：
先開「小美」，再開「英文」，拿取排在最前面的第 0 個寶物。
`grade["小美"]["英文"][0]` ➡️ 得到 `93`

### 🧮 算術魔法：算平均分數

* **`sum(...)`**：把這堆分數全部加起來（像把零錢倒在一起數總共多少）。
* **`len(...)`**：數數看這堆分數有幾個（像數有幾個銅板）。
* **平均值**：總分除以個數 `sum(成績) / len(成績)`。

```python
# 幫每位同學算國文的平均分數
for name, subjects in grade.items():
    chinese = subjects["國文"]             # 拿到國文成績
    avg = sum(chinese) / len(chinese)     # 算出平均
    print(f"{name}的國文平均是 {avg:.2f} 分！")  # .2f 表示小數點後只留2位喔！

```

恭喜你！你已經把今天上課的 Python 魔法咒語都複習完畢囉！是不是非常好懂呢？繼續加油！🚀
    """)

with st.expander("Class 4 課堂筆記"):
    st.write(
        """
這是一份專門為你設計的 **Python 魔法筆記**！我們用最簡單、好懂的方式，把今天學到的厲害指令通通記下來。準備好我們就開始囉！✨

---

## 🚂 第一站：自動幫你重複做事的「迴圈小火車」

當你想要電腦幫你重複畫畫、數數，寫很多次同樣程式碼太累了！這時候就要派出 **`for` 迴圈**。

### 1. `range()`：小火車的神秘軌道

`for i in range(...)` 就像一列小火車，`i` 是火車駕駛員（也可以叫別的名字），它會沿著 `range()` 鋪好的軌道一站一站載數字。

* **單向軌道 `range(5)**`：
火車會從 `0` 開始出發，**但是最後一站不載人**（不包含 5）！
所以載到的數字是：`0, 1, 2, 3, 4`。
* **指定起點與終點 `range(1, 5)**`：
火車從 `1` 出發，一樣不包含終點 `5`！
所以載到：`1, 2, 3, 4`。
* **跳格軌道 `range(1, 10, 2)**`：
從 `1` 出發，每次「走 2 步」（步進值），一樣不包含 `10`。
所以載到：`1, 3, 5, 7, 9`（像在跳格子一樣）。

```python
# 這是你學到的範例：把火車載到的數字通通乘兩倍！
for i in range(5):
    a = i * 2  # 把載到的數字乘以 2 存進 a
    print(a)   # 畫面上會出現：0, 2, 4, 6, 8

```

---

## 🎈 第二站：網頁神奇魔法與「猜數字遊戲」

我們可以用 `Streamlit` 這個神奇黏土，在網頁上做出可以按的按鈕、輸入框，甚至是好玩的遊戲！

### 1. 網頁的記憶吐司 `st.session_state`

一般的網頁只要一按按鈕就會「失憶」（重新整理），但如果我們用 `st.session_state`，網頁就會像吃了記憶吐司一樣，把重要的事情（比如：秘密數字、猜了幾次）牢牢記住！

```python
# 檢查如果「記憶」裡還沒有秘密數字，就隨機選一個 1~100 的數字記起來
if "secret_number" not in st.session_state:
    st.session_state.secret_number = random.randint(1, 100)

```

當玩家猜對時，我們還可以用 `st.balloons()` 魔法，在網頁上**噴出繽紛的慶祝氣球**喔！🎈

---

## 🔑 第三站：貼了標籤的百寶箱「字典 dict」

平常我們用列表（List）裝玩具，但當東西太多時，找起來很麻煩。**字典（dict）**就像一個百寶箱，裡面有很多抽屜，每個抽屜都有**專屬的鑰匙（Key）**，鎖著**珍貴的寶物（Value）**。

> **黃金規則：**
> * 鑰匙（Key）不能重複，必須是獨一無二的！
> * 寶物（Value）可以是任何東西，重複也沒關係。
> 
> 

### 1. 字典基本功法

我們用一個簡單的百寶箱 `d = {"a": 1, "b": 2, "c": 3}` 來練習：

* **看有哪些鑰匙？** `d.keys()`：拿出一整串鑰匙（會拿到 `"a"`, `"b"`, `"c"`）。
* **看有哪些寶物？** `d.values()`：打開所有抽屜看裡面裝什麼（會拿到 `1`, `2`, `3`）。
* **塞新寶物進去**：
```python
d["d"] = 4  # 新增一個叫 "d" 的抽屜，裝進寶物 4
d["a"] = 5  # 把舊抽屜 "a" 裡面的寶物換成全新的 5

```


* **丟掉抽屜** `d.pop("a")`：把 `"a"` 抽屜連同裡面的寶物一起丟掉！
* **檢查鑰匙在不在？** `in` 偵探：
```python
print("a" in d)  # 檢查百寶箱裡有沒有 "a" 的鑰匙？有就回答 True（對），沒有就回答 False（錯）

```



---

## 🏫 第四站：大挑戰！學校成績超級搜尋大師

現在，我們要用百寶箱字典來裝「好朋友們的成績」。這是一個「百寶箱裡還有小百寶箱」的超級字典（巢狀字典）：

```python
# 每個同學的抽屜裡，又裝了國文、數學、英文三個學科的成績抽屜！
grade = {
    "小明": {"國文": [90, 80, 70], "數學": [85, 75, 65], "英文": [95, 85, 75]},
    "小美": {"國文": [88, 78, 68], "數學": [83, 73, 63], "英文": [93, 83, 73]},
}

```

### 🎯 尋寶特攻隊：怎麼拿到特定成績？

1. **找出小明的數學成績**：
先開「小明」的抽屜，再開裡面的「數學」抽屜。
`grade["小明"]["數學"]` ➡️ 得到 `[85, 75, 65]`
2. **找出小美第一次（第 0 個）英文成績**：
先開「小美」，再開「英文」，拿取排在最前面的第 0 個寶物。
`grade["小美"]["英文"][0]` ➡️ 得到 `93`

### 🧮 算術魔法：算平均分數

* **`sum(...)`**：把這堆分數全部加起來（像把零錢倒在一起數總共多少）。
* **`len(...)`**：數數看這堆分數有幾個（像數有幾個銅板）。
* **平均值**：總分除以個數 `sum(成績) / len(成績)`。

```python
# 幫每位同學算國文的平均分數
for name, subjects in grade.items():
    chinese = subjects["國文"]             # 拿到國文成績
    avg = sum(chinese) / len(chinese)     # 算出平均
    print(f"{name}的國文平均是 {avg:.2f} 分！")  # .2f 表示小數點後只留2位喔！

```

恭喜你！你已經把今天上課的 Python 魔法咒語都複習完畢囉！是不是非常好懂呢？繼續加油！🚀
    """)

with st.expander("Class 5 課堂筆記"):
    st.write(
        """
這就為你把剛才整合後的購物平台與 AI 客服 Streamlit 程式碼，整理成一份結構清晰、方便隨時複習的 **開發筆記**！

---

# 📝 開發筆記：智慧購物平台與 AI 客服整合

這份專案展示了如何利用 **Streamlit** 的狀態管理（`st.session_state`）與 **OpenAI API** 建立一個雙欄位、即時連動的「電商平台 + AI 客服助理」。

---

## 🏗️ 1. 系統架構設計

網頁版面採用 **雙欄響應式配置（6:4）**，讓使用者一邊購物、一邊能與 AI 進行即時互動。

```
+-------------------------------------------------------------+
|                     智慧購物平台與 AI 客服                    |
+------------------------------+------------------------------+
|                              |                              |
|   🛒 左側：購物與管理區 (6)    |   ✨ 右側：AI 客服助手 (4)     |
|                              |                              |
|   1. 商品展示 (卡片/圖片/按鈕)  |   1. 客服進階設定 (折疊區)     |
|   2. 購買紀錄                 |   2. 歷史對話視窗 (滾動容器)   |
|   3. 補貨管理區               |   3. 即時資料對話輸入框        |
|   4. 即時庫存統計 (Metric)    |                              |
|                              |                              |
+------------------------------+------------------------------+

```

---

## 🧠 2. 關鍵技術重點

### A. Session State（狀態保存）

Streamlit 每次使用者操作（如點擊按鈕）都會「重新運行」整份程式碼。為了避免資料遺失，專案初始化了四個關鍵狀態：

* `st.session_state.inventory`：儲存商品基本資訊（價格、當前庫存、圖片路徑）。
* `st.session_state.purchase_history`：儲存使用者的購買歷程。
* `st.session_state.history`：儲存 AI 聊天室的對話紀錄。
* `st.session_state.system_message`：儲存 AI 的初始角色設定。

### B. AI 數據即時同步（Context Injection）

這是本專案最核心的亮點！當使用者送出訊息時，程式**不會**直接送出，而是先將**當前即時庫存與購買紀錄**包裝進 `System Prompt` 中：

```python
# 動態注入即時數據
current_inventory_status = "\n".join([
    f"- {info['name']}: 剩餘庫存 {info['stock']} 件"
    for info in st.session_state.inventory.values()
])

dynamic_system_prompt = (
    f"{st.session_state.system_message}\n\n"
    f"【當前商店即時數據】\n"
    f"商品庫存狀態：\n{current_inventory_status}"
)

```

> 💡 **效果**：這讓 AI 客服能擁有「店面感知能力」。當用戶問 *「香蕉還剩幾件？」*，AI 能夠準確回答資料庫裡的真實數字，而不會瞎掰。

---

## 📂 3. 專案目錄結構建議

在執行此 Streamlit 應用程式時，建議的本機資料夾結構如下：

```text
my_shopping_app/
├── .streamlit/
│   └── secrets.toml     # 存放 OpenAI API Key
├── image/
│   ├── apple.png        # 蘋果商品圖片
│   ├── banana.png       # 香蕉商品圖片
│   ├── bg.png           # 備用圖片
│   └── orange.png       # 橘子商品圖片
└── app.py               # Streamlit 程式碼主檔案

```

> **金鑰設定祕笈（`.streamlit/secrets.toml`）：**
> ```toml
> OPENAI_API_KEY = "your-actual-api-key-here"
> 
> ```
> 
> 

---

## 🛠️ 4. 亮點 UI 元件應用

| 模組/元件 | 程式碼語法 | 筆記與用途說明 |
| --- | --- | --- |
| **雙欄排版** | `st.columns([6, 4])` | 將畫面完美分割，左邊放購物台，右邊放對話區。 |
| **狀態重整理** | `st.rerun()` | 當庫存或對話改變時，強制重新渲染網頁以即時顯示新狀態。 |
| **對話紀錄框** | `st.container(height=500)` | 限制對話視窗的高度為 `500px` 且可滾動，避免畫面被無限拉長。 |
| **卡片邊框** | `st.container(border=True)` | 在商品展示區外加上細線邊框，呈現類似電商卡片（Card）的質感。 |
| **小工具/提示** | `st.toast()` / `st.metric()` | `toast` 用於右上角彈出購買成功的通知；`metric` 用於底部顯示精美的庫存看板。 |

---

## 🚀 5. 未來可擴充方向

1. **購物車功能**：目前是「點擊直接購買（庫存減一）」，未來可以擴充 `st.session_state.cart`，實作「加入購物車 ➔ 一併結帳」的功能。
2. **AI 代客下單**：透過 OpenAI 的 **Function Calling (Tool Use)** 讓 AI 客服可以直接幫忙執行 `buy_product` 的語法。例如使用者說「幫我買一顆蘋果」，AI 就直接扣除庫存！

---

這份筆記把最核心的**資料同步邏輯**和**畫面配置**都條理化了，非常適合當作範本！下次要開發類似的專案時，直接開這篇筆記就能秒懂結構囉！
""")